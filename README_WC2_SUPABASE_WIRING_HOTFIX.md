# IzyTel WC2 - Supabase wiring hotfix

Correctif minimal: remplace `firestore_customer_order_repository.dart` par la version WC2 qui délègue la récupération par code à `SupabaseCustomerOrderRecoveryRepository`.

Aucune modification de `firestore.rules`.
Aucun déploiement Firebase Rules.
